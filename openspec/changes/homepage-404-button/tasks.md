## 1. Homepage button implementation

- [x] 1.1 Add a clearly labeled 404 button to `index.html` in the main content area.
- [x] 1.2 Configure the button to navigate to `/this-page-does-not-exist` in the same tab using semantic HTML (`<a class="btn">`).

## 2. Styling and verification

- [x] 2.1 Ensure button spacing and visual treatment match existing homepage patterns without layout regressions.
- [x] 2.2 Run `npm run check` and confirm the homepage button behavior matches the spec scenarios.
